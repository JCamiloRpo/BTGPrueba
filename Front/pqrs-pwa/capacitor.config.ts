import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.btg.prueba.app',
  appName: 'pqrs-pwa',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
